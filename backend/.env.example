# ============================================
# APPLICATION CONFIGURATION
# ============================================
APP_NAME="Lege Aplicata"
ENVIRONMENT=production  # development, staging, production

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL Database
#
# FOR LOCAL DEVELOPMENT (Windows):
# Use your local network IP where PostgreSQL is running
# Example: PG_HOST=192.168.1.30, PG_PORT=5432
#
# FOR PRODUCTION (Ubuntu Server):
# If PostgreSQL runs on the same server: PG_HOST=localhost or 127.0.0.1
# If external: Use the actual IP/hostname
# Example: PG_HOST=localhost, PG_PORT=5432

PG_HOST=localhost
PG_PORT=5432
PG_USER=your_database_user
PG_PASS=your_database_password_here
PG_DB=verdict

# Database URLs (constructed from above)
# Note: All databases use the same PostgreSQL instance
DATABASE_URL=postgresql://${PG_USER}:${PG_PASS}@${PG_HOST}:${PG_PORT}/${PG_DB}
DATABASE_CODURI_URL=postgresql://${PG_USER}:${PG_PASS}@${PG_HOST}:${PG_PORT}/coduri_juridice
DATABASE_MODELE_URL=postgresql://${PG_USER}:${PG_PASS}@${PG_HOST}:${PG_PORT}/modele_acte

# ============================================
# OLLAMA AI SERVICE
# ============================================
# FOR LOCAL DEVELOPMENT (Windows):
# Use your local network IP where Ollama is running
# Example: OLLAMA_URL=http://192.168.1.30:11434
#
# FOR PRODUCTION (Ubuntu Server):
# If Ollama runs on the same server: http://localhost:11434
# If external: Use the actual IP/hostname
OLLAMA_URL=http://localhost:11434

# ============================================
# CORS CONFIGURATION
# ============================================
# Comma-separated list of allowed origins
#
# FOR LOCAL DEVELOPMENT (Windows):
# CORS_ORIGINS=http://localhost:5174,http://localhost:5173,http://127.0.0.1:5174,http://127.0.0.1:5173
#
# FOR PRODUCTION (Ubuntu Server - chat.legeaaplicata.ro):
# CORS_ORIGINS=https://chat.legeaaplicata.ro,http://chat.legeaaplicata.ro
#
# FOR BOTH (recommended during transition):
# CORS_ORIGINS=http://localhost:5174,http://localhost:5173,https://chat.legeaaplicata.ro,http://chat.legeaaplicata.ro

CORS_ORIGINS=https://chat.legeaaplicata.ro,http://chat.legeaaplicata.ro

# ============================================
# AUTHENTICATION & SECURITY
# ============================================
# Credentials for /setari admin page
USER_SETARI=your_admin_username
PASS_SETARI=your_secure_password

# JWT Secret Key (generate a strong random key)
# Generate with: openssl rand -hex 32
SECRET_KEY=your_secret_key_here_change_this_in_production

# ============================================
# FILE STORAGE
# ============================================
UPLOAD_DIR=/app/uploads
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes

# ============================================
# LOGGING
# ============================================
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=/app/logs/backend.log

# ============================================
# PERFORMANCE & LIMITS
# ============================================
WORKERS=4  # Number of Gunicorn workers (2-4 x NUM_CORES)
MAX_CONNECTIONS=100
TIMEOUT=60

# ============================================
# FRONTEND PORT (for Docker deployment)
# ============================================
FRONTEND_PORT=8080
