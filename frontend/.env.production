# Production Environment Configuration
# This file is used when running: npm run build

# API URL - empty means use relative paths (nginx proxy handles routing)
# In production, nginx will proxy /api requests to the backend
VITE_API_URL=
